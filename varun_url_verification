import json
import requests

def verify():
    
    url = ''
    req = requests.get(url)
    
    #404 error
    if req.status_code == 404:
        print ("Invalid Link: 404 error")
    #
    elif 
        try:
          print(x)
        except NameError:
          print("Not Defined")
        except:
          print("Something else went wrong")
    else:
        print ("Usuable, Download Data")

        
verify()
